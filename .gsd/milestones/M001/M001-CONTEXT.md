# M001 Context

## Milestone

CampusNav v1.6 GPS Integration & UX Refinements.

## Goal

Add GPS-based "you are here" positioning and improve route clarity and admin editing workflows, while preserving the shipped v1.5 multi-floor baseline.

## Scope

- Touch gesture fixes (pinch focal point + rotation pivot/jitter threshold)
- Multi-floor direction section dividers and floor-transition language
- Admin visual floor-connector linking workflow
- GPS bounds configuration for campus/floors
- Student GPS dot + nearest-node snap + graceful fallback behavior

## Dependencies

No upstream milestone dependency inside this migrated `.gsd` tree. This milestone assumes the v1.5 shipped baseline from the legacy `.planning` history.
