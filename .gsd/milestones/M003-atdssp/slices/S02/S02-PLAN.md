# S02: Bottom Sheet + Floating UI Chrome

**Goal:** Build the structural UI chrome: BottomSheet with 3 snap points (collapsed 120px / half 300px / full 600px), FloatingSearchBar pill, FloatingFloorSwitcher, and AccessibleToggle. These form the floating layer of the new layout.
**Demo:** After this: Bottom sheet renders and can be dragged between collapsed (strip), half (route preview), and full (settings) snap points. Floating search bar pill is visible at top of screen.

## Tasks
