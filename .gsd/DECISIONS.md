# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001/S24 planning | routing-ui | Cross-floor direction semantics and UI floor section contract | Determine up/down transitions from resolved floor numbers (falling back to floorId only when floor metadata is missing) and include floorId/floorNumber on each DirectionStep for section rendering. | Database floor IDs are identifiers, not reliable vertical ordering. Using floor numbers avoids incorrect up/down language, and carrying floor metadata on steps lets the sheet render deterministic floor sections without extra graph lookups. | Yes | agent |
| D002 | M001/S25 planning | integration | Atomic persistence strategy for admin floor-connector linking | Implement a dedicated protected endpoint (`POST /api/admin/connectors/link`) that validates source/target compatibility and writes reciprocal connector fields for both nodes in one DB transaction, including relink/unlink cleanup. | The existing save flow serializes the active floor separately from off-floor data; a dedicated endpoint guarantees bilateral consistency at write time and provides explicit failure diagnostics, preventing one-sided cross-floor links. | Yes | agent |
| D003 | M001/S25 execution | state-management | How admin editor applies connector link/unlink updates returned from `/api/admin/connectors/link` | Treat connector updates as server-authoritative and apply full-node replacements (`REPLACE_NODES`) rather than partial merges so unlink responses can truly clear optional connector fields. | `NavNode` connector fields are optional; merge-based updates can leave stale `connectsTo*` values after unlink operations. Replacing full node entries from backend `updatedNodes` keeps active-floor and cached graph state consistent with atomic server writes. | Yes | agent |
| D004 | M001/S26 planning | integration | Admin GPS bounds persistence contract and client synchronization model | Add a dedicated protected floor-scoped endpoint (`PUT /api/admin/floors/:id/gps-bounds`) that accepts only complete set/clear tuples and treat the response as server-authoritative when patching floor metadata in editor state. | GPS calibration is floor metadata consumed across admin and student flows. A floor-scoped write path with deterministic validation diagnostics prevents partial tuple drift and keeps `GET /api/map` contract data trustworthy for downstream geolocation projection. | Yes | agent |
| D005 | M001/S26 execution | frontend-validation | How admin GPS bounds form computes validity, payload shape, and save readiness across rows | Centralize GPS bounds parsing/validation/payload derivation in pure helpers (`gpsBoundsForm.ts`) and derive per-row save gating from helper output (`deriveGpsBoundsRowUiState`) instead of ad hoc component-level checks. | A shared pure ruleset keeps inline error messaging, save-button disabling, and request payload generation deterministic and testable. This avoids drift between UI and API expectations (complete tuple or full clear tuple, strict min/max ordering) and gives S27 a stable floor-metadata contract. | Yes | agent |
| D006 | M001/S27 override 2026-03-24T12:13:48.431Z | workflow | Execution order for active milestone work before research begins | Create a checkpoint commit for current changes before any research or deep-dive analysis in active slices; this supersedes the prior implicit workflow where research could start before a commit checkpoint. | User override requires commit-first sequencing to preserve rollback safety, keep research deltas traceable, and avoid mixing exploratory edits with uncheckpointed work. | Yes | human |
| D007 | M001/S27/T03 | student-gps-ui-state | How to derive “Use my location” readiness and fallback messaging in student mode | Centralize derivation in `deriveStudentGpsState`, keyed by floor calibration, geolocation status, confidence threshold (50m), and nearest-node availability; expose only derived UI state to `SearchOverlay`. | A single pure derivation layer keeps fallback reason/action enablement deterministic, testable, and redaction-safe while allowing `FloorPlanCanvas` to focus on snap wiring (`setStart`) rather than duplicating UI-state branching. | Yes | agent |
| D008 | M001/S27/T02 | student-gps-runtime | How browser geolocation watch lifecycle and marker rendering gates are structured in student map mode | Use a pure `startGeolocationWatch` lifecycle helper wrapped by `useGeolocation`, and only enable/render GPS marker state on floors with calibrated `gpsBounds` plus confident fixes (<=50m) using shared projection/confidence helpers. | Centralizing watch subscribe/error/teardown behavior in a pure helper keeps status mapping deterministic and testable without DOM harnesses. Restricting marker derivation to calibrated floors + confident fixes prevents misleading dots/rings and enforces the same confidence contract across hook, canvas, and overlay state. | Yes | agent |
| D009 | M002-a5gn45 discuss | architecture | Mobile runtime implementation strategy for CampusNav app version | Build a full native React Native app runtime (iOS + Android) instead of wrapping the existing web app in Capacitor. | User explicitly selected a full native rebuild to reach maps-like interaction quality and avoid web-wrapper constraints. | Yes — if execution evidence shows parity is achievable with lower-risk architecture. | human |
| D010 | M002-a5gn45 discuss | integration | Positioning and reliability contract for real-time guidance | Use GPS + device sensors only for positioning in this initiative, with confidence-gated guidance and explicit fallback prompts when confidence is low. | User selected GPS+sensor-only strategy and accepted confidence-gated behavior as the trust-preserving mechanism. | Yes — if on-device evidence shows unacceptable reliability and infrastructure options become viable. | human |
| D011 | M002-a5gn45 discuss | scope | M002 feature-boundary for admin capabilities | Mobile app scope is student navigation only; admin editing remains in the existing web admin panel. | User selected 'Student app + web admin' to keep milestone focused on visitor navigation outcomes. | Yes — if a later milestone needs mobile admin support. | human |
| D012 | M002-a5gn45 discuss | operability | Runtime mode for first real-time guidance release | M002 real-time guidance runs in foreground mode; full background/lock-screen continuity is deferred. | User chose foreground-first delivery to reduce complexity while proving core live guidance loop. | Yes — revisit after foreground reliability is proven. | human |
| D013 | M002-a5gn45 discuss | pattern | Phasing of guidance experience enhancements | Voice prompts and haptic cues are mandatory for the full vision but deferred to a later milestone after core reliability is stable. | User marked voice+haptics as non-negotiable overall but agreed to defer from M002 to protect core delivery risk. | Yes — if core reliability lands earlier than expected. | human |
| D016 | M002-a5gn45/S03/T01 | geometry | bearing() formula for screen-space navigation | `atan2(dy, -dx) * (180/π) + 270`, normalized to [0, 360) — correctly maps screen-space coords where north=0°, east=90°, south=180°, west=270°. | Screen-space y-axis orientation is inverted from Cartesian; x-axis mirroring is required to produce correct cardinal mapping. Initial attempts with `atan2(dx, -dy)` and `atan2(dy, dx)` both produced 90° rotation on east-west axis. | Yes | agent |
| D017 | M002-a5gn45/S03/T02 | guidance-confidence | Confidence 'low' vs 'none' boundary | 'low' = GPS finite but unconfident (accuracy > maxAccuracy); 'none' = GPS accuracy is null or infinite. | Spec exactly requires 'none' only when accuracy is absent or infinite; 'low' is the intermediate for poor-but-present GPS fixes. | Yes | agent |
| D018 | M002-a5gn45/S03/T04 | state-management | Mutable ref + useState snapshot pattern for high-frequency GPS updates | Position updates written to a `processPositionRef` mutable ref on every tick; stable `useState` snapshot commits to React only after each processing pass completes. | High-frequency GPS updates (2s interval) would cause React render thrashing if each tick triggered a re-render. Ref + snapshot decouples the update loop from the render cycle. | Yes | agent |
| D019 | M002-a5gn45/S03/T04 | guidance-reroute | useEffect on guidanceState.phase drives async reroute | `useEffect([guidanceState.phase], ...)` triggers reroute computation when phase becomes 'rerouting'. Phase is stable across renders; direct ref reading in effect avoids stale closure. | Reroute is asynchronous (pathfinding takes time). Polling the ref inside an effect keyed on phase (the stable render-committed value) is more reliable than trying to read a ref directly inside another callback. | Yes | agent |
| D020 | M002-a5gn45/S03/T02 | coordinate-resolution | Two-phase nodeById lookup | Tests use `PathWithNodeById` shape (extends PathResult); production uses `route.path.nodeById` from NormalizedNavGraph. Functions access `path.nodeById ?? route.path.nodeById` as fallback. | Bridging test shapes (extended PathResult with nodeById) and production shapes (NormalizedNavGraph nodeById on route.path) avoids type casting and keeps functions work-correct without conditional branches. | Yes | agent |
| D021 | M002-a5gn45/S03/T04 | module-architecture | Lazy require inside function body | `const { recomputeDirections } = require('recomputeDirections')` called inside the reroute function, not at module top level. | Avoids circular module dependency at initialization time. The require is cheap since module is already loaded by the time reroute fires. | Yes | agent |
| D022 | M002-a5gn45/S03/T03 | sensor-smoothing | EMA alpha=0.3 with shortest-angular-path wraparound | `smoothed = alpha * newHeading + (1-alpha) * smoothed` where alpha=0.3; wraparound uses `|delta| > 180` shortcut. | α=0.3 balances responsiveness vs. jitter for heading data. Shortest-path wraparound (270°→0° goes via -90° not +90°) prevents 360° oscillation at cardinal boundaries. | Yes | agent |
| D014 | M002-a5gn45/S01/T02 | mobile-domain | How mobile NavGraph normalization should represent non-accessible edge weights from backend payloads | Preserve raw `accessibleWeight` from `/api/map` payload and add derived `effectiveAccessibleWeight` that is `Infinity` when `accessible=false`. | Backend payloads may serialize non-accessible edges with large finite sentinels (e.g., 1e10) because JSON cannot encode Infinity. Keeping raw values preserves transport fidelity while derived effective weights keep accessibility routing semantics explicit for downstream consumers. | Yes | agent |
| D015 |  | architecture | Custom A* pathfinding over ngraph.graph for React Native | Implemented MobilePathfindingEngine directly on NormalizedNavGraph | ngraph.graph is an unverified dependency for React Native. A* on NormalizedNavGraph is simple, fast (CPU microseconds for campus graphs), and fully testable without native module compatibility concerns. | Yes | agent |
| D023 | M002-a5gn45/S04/T01 | map-rotation | Heading rotation phase-gated by guidance phase | Pass `headingDegrees={null}` during idle phase to preserve manual map exploration; pass actual heading only when `guidanceState.phase !== 'idle'`. | Prevents map auto-rotation from interfering with user's manual map orientation before guidance starts. Phase gating ensures heading rotation is an enhancement layered on top of manual exploration, not a replacement. | Yes | agent |
| D024 | M002-a5gn45/S04/T01 | map-transform | Cumulative rotation via manual + heading composition | `rotateZ: ${transform.rotationDeg + (headingRotationDeg ?? 0)}deg` combines manual rotation (pinch-rotate) and heading rotation in MapViewport. | Heading is additive to manual rotation, not a replacement. Preserving headingRotationDeg through all transform mutations (applyPanDelta via spread, applyPinchRotate explicit) prevents heading value loss during gestures. | Yes | agent |
| D025 | M002-a5gn45/S04/T02 | floor-tracking | previousFloorIdRef pattern for floor boundary detection | Use `useRef<number | null>` to track previous floor; emit floor-transition event only when both current and previous are non-null and different. | Ref-based tracking avoids triggering re-renders during each position fix. Event fires only on genuine floor crossings, not on initial position establishment. Pattern decouples detection logic from rendering. | Yes | agent |
| D026 | M002-a5gn45/S04/T02 | floor-ui | FloorTransitionBanner uses useRef for prevFloorId | `useRef<number | null>(null)` initialized on mount, compared against current `props.floorId` to detect transitions. | State-free transition detection: ref captures previous value without causing re-renders. Component renders banner only when transition is detected, auto-hides after 2500ms via setTimeout. | Yes | agent |
| D027 | M002-a5gn45/S04/T02 | guidance-state | currentFloorId reset on stopGuidance | `stopGuidance` action sets `currentFloorId` to null to match idle state semantics. | Idle guidance state should have no floor context. Resetting on stop ensures guidance state is clean and consistent, matching the null-initialized behavior on first load. | Yes | agent |
| D028 | M002-a5gn45/S04/T03 | accessibility-ui | Amber highlighting for accessible mode | Elevator/ramp icons get `backgroundColor: '#facc15'` (amber) + dark icon color; instruction text appends ' (accessible)' when `accessibleMode === true`. | Visual distinction for accessible routing options helps wheelchair users quickly identify accessible paths. Amber provides strong contrast against navy default while remaining visually distinct from error/warning states. | Yes | agent |
| D029 | M002-a5gn45/S04/T03 | typescript-strictness | safeFloorId coercion pattern | `const safeFloorId: number | null = floorId ?? null` introduced in FloorTransitionBanner to satisfy TypeScript strict mode for useRef/setState. | Optional props (`floorId?: number | null`) require explicit coercion to union types before passing to useRef. This pattern is explicit and self-documenting compared to nullish coalescing inline. | Yes | agent |
